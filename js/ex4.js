//Prompt user to ask for a wor duntil the user types "stop". If stop is the word
//display an appropriate message.

 var word = [];

 while (word != stop) {
   word.push(word);
   word.push(prompt("Enter a word"));
   

 }
     


console.log(word);